// Uncomment if using Node
var chai = require('chai');
var assert = chai.assert;

describe('Array', function() {
  it('should start empty - is exemple', function() {
    var arr = [];

    setTimeout(() => { assert.equal(arr.length, 0)} , 1);
  });
  it('should start empty - is exemple', function() {
    var arr = [];

    setTimeout(() => { assert.equal(arr.length, 0)} , 2);
  });
  it('should start empty - is exemple', function() {
    var arr = [];

    setTimeout(() => { assert.equal(arr.length, 0)} , 3);
  });
  it('should start empty - is exemple', function() {
    var arr = [];

    setTimeout(() => { assert.equal(arr.length, 0)} , 4);
  });

});