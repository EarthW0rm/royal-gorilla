steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/royal-gorilla:$BRANCH_NAME', '.']
  env:
  - NODE_ENV=production

images: ['gcr.io/$PROJECT_ID/royal-gorilla:$BRANCH_NAME']